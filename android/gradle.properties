org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# Windows 下项目盘符与 Pub 缓存盘符不同会触发 Kotlin 增量缓存异常，优先保证构建稳定性。
kotlin.incremental=false

# Local proxy settings must not be committed here.
# If needed for a developer machine, put them in ~/.gradle/gradle.properties.
